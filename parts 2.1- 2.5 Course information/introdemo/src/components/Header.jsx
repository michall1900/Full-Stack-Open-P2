/**
 * The Header component is responsible for rendering the course name as a header.
 * 
 * @param {Object} props - The props object containing the course name.
 * @param {string} props.course - The name of the course to be displayed.
 */
const Header = (props) => <h1>{props.course}</h1>


export default Header;